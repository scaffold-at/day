{
  "name": "@scaffold/feedback-worker",
  "version": "0.2.3",
  "private": true,
  "description": "Cloudflare Worker that receives scaffold-day feedback POSTs and forwards to a Discord channel webhook (S66).",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "wrangler": "^3.0.0"
  }
}
