---
title: scaffold-day docs
description: "emit a single AI-readable dump of the scaffold-day surface"
command: docs
---

{/* AUTOGENERATED by `bun run gen:cli-reference`. Do not edit by hand. */}
{/* Source: registry + 6-section help (SLICES §S53.8). */}

# `scaffold-day docs`

> emit a single AI-readable dump of the scaffold-day surface

## WHAT

`docs --for-ai` returns the entire surface — CLI commands + MCP tools + JTBD recipes — in markdown / json / yaml so an AI client can paste it once and stay token-efficient.

## WHEN

When booting a fresh AI session that needs to reason about scaffold-day. Or as the source of `AGENTS.md` (§S53.7) and the docs site (§S53/§S53.8).

## COST

Local, in-memory. No file I/O beyond reading the registry.

## INPUT

```
--for-ai [--format markdown|json|yaml] [--cli-only|--mcp-only] [--commands name,name,...]
```

## RETURN

Stdout. Exit 0.

## GOTCHA

v0.1 emits ~10K char markdown for the full bundle (well within Claude's window). Use --commands to fetch a sliver. Tracking SLICES.md §S53.5 (cmd) / §S53.7 (AGENTS.md generator) / §S53.8 (CLI reference).
