---
title: scaffold-day morning
description: "record today's morning anchor (the t=0 of the relative time model)"
command: morning
---

{/* AUTOGENERATED by `bun run gen:cli-reference`. Do not edit by hand. */}
{/* Source: registry + 6-section help (SLICES §S53.8). */}

# `scaffold-day morning`

> record today's morning anchor (the t=0 of the relative time model)

## WHAT

Record (or query) today's morning anchor — the wall-clock instant the user 'started today'. The anchor is the t=0 reference for sleep_budget (S58), cognitive_load decay (S59), and rest-break suggestion (S61). v0.1 storage: append-only `<home>/logs/heartbeats.jsonl`; the latest entry for a given date wins.

## WHEN

First action of the day, or when an AI client receives a 'good morning' message and emits MCP `record_morning`. Auto-fallback: if no explicit call landed yet, the first scaffold-day CLI / MCP invocation of the day records `source: "auto"`.

## COST

Local file I/O only. Append-only single-line write to `logs/heartbeats.jsonl`. No network.

## INPUT

```
[--at HH:MM | --at <ISO 8601 + TZ>] [--force] [--json]
```

## RETURN

Exit 0 on success. If today's anchor is already set and --force is not given, prints a non-error notice and exits 0. JSON shape: &#123; anchor, date, source, was_already_set, forced &#125;.

## GOTCHA

Auto-fallback runs on every command (except `morning` itself); explicit `morning` always wins by replacing or no-op'ing the auto entry depending on --force. Tracking SLICES.md / issue #3 §S60.
