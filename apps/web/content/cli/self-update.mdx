---
title: scaffold-day self-update
description: "check for and install a newer scaffold-day binary"
command: self-update
---

{/* AUTOGENERATED by `bun run gen:cli-reference`. Do not edit by hand. */}
{/* Source: registry + 6-section help (SLICES §S53.8). */}

# `scaffold-day self-update`

> check for and install a newer scaffold-day binary

## WHAT

Resolve the latest scaffold-day release from scaffold.at/day/latest, verify the SHA-256, and replace the running binary. Auto-update is OFF by default; this command is the only path.

## WHEN

Periodically, or when release notes mention a fix you need.

## COST

One HTTPS GET to the latest pointer plus a binary download (~60 MB). No telemetry.

## INPUT

```
[--check] check only, no install. [--rollback] revert to the previous binary.
```

## RETURN

Exit 0 with the new version printed. Exit non-zero with a clear cause/try/docs block on failure.

## GOTCHA

Refuses to run if launched from a package manager path (Homebrew, etc.) where you should update via that manager. Tracking SLICES.md §S47.
