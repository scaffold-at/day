---
title: scaffold-day policy
description: "show / patch / preset-apply the local scheduling policy"
command: policy
---

{/* AUTOGENERATED by `bun run gen:cli-reference`. Do not edit by hand. */}
{/* Source: registry + 6-section help (SLICES §S53.8). */}

# `scaffold-day policy`

> show / patch / preset-apply the local scheduling policy

## WHAT

Read or modify <home>/policy/current.yaml. v0.1 ships subcommands: `show` (print the YAML or compiled JSON), `patch <json-patch>` (apply RFC 6902 ops while preserving comments), `preset apply <name>` (overwrite with a built-in preset; v0.1 has only `balanced`).

## WHEN

After install (seed via `preset apply`), or whenever you change scheduling rules / weights.

## COST

Local file I/O. Patch parses the YAML through the yaml Document API so unchanged regions stay byte-identical.

## INPUT

```
show [--json]
patch <json-patch> | --from-stdin [--json]
preset apply <name>
```

## RETURN

Exit 0 on success. DAY_NOT_INITIALIZED if there's no policy file yet. DAY_INVALID_INPUT on a bad patch / preset name. DAY_USAGE on missing args.

## GOTCHA

Comments in `current.yaml` survive `policy patch`. `preset apply` OVERWRITES the file — back up first if you've customized it. Tracking SLICES.md §S15 (cmds), §S14 (codec), §S13 (schemas).
